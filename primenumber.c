#include <stdio.h>
int main()
{
    int i, n, f;
    for (i = 1; i <= n; i++)
        ;
    printf("Enter the Value : ");
    scanf("%d", &n);
    {
        if (n % i == 0)
            f = 0;
        f++;
    }

    if (f == 2)
        printf("Prime Number");
}